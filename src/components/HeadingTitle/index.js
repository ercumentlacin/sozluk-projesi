import React from "react";
import { StyledHeadingTitle } from "./style";

const HeadingTitle = ({ title }) => {
  return <StyledHeadingTitle>{title}</StyledHeadingTitle>;
};

export default HeadingTitle;
